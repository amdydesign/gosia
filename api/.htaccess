<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Fix for Authorization header being stripped by Apache (dHosting/CGI)
    RewriteCond %{HTTP:Authorization} ^(.*)
    RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]
</IfModule>
